import { createContext } from "react";

export const Image = createContext();
export const HabitsConcluded = createContext();
export const HabitsTodayList = createContext();
export const Token = createContext();
export const Language = createContext();
